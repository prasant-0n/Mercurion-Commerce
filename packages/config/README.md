# Shared Config Package

Reserved for reusable monorepo configuration and shared runtime defaults.

Planned contents:

- shared TypeScript presets
- shared ESLint fragments if repo complexity grows
- environment contract helpers for multiple apps
